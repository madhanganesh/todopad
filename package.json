{
  "name": "todopad",
  "version": "1.0.0",
  "description": "sqlx database create sqlx migrate run",
  "main": "index.js",
  "scripts": {
      "build:css": "npx tailwindcss -i ./tailwind.css -o ./static/tailwind.css --minify",
    "watch:css": "npx tailwindcss -i ./tailwind.css -o ./static/tailwind.css --watch",
    "build:js": "npx esbuild static/main.js --bundle --minify --format=iife --outfile=static/js/main.js",
    "watch:js": "npx esbuild static/main.js --bundle --format=iife --outfile=static/js/main.js --watch",
    "build:fa": "mkdir -p static/fontawesome && cp -r node_modules/@fortawesome/fontawesome-free/css static/fontawesome && cp -r node_modules/@fortawesome/fontawesome-free/webfonts static/fontawesome",
    "build": "npm run build:css && npm run build:js && npm run build:fa",
    "watch": "npm run watch:css & npm run watch:js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fortawesome/fontawesome-free": "^6.7.2",
    "@tailwindcss/cli": "^4.0.3",
    "alpinejs": "^3.14.8",
    "autoprefixer": "^10.4.20",
    "esbuild": "^0.24.2",
    "htmx.org": "^1.9.12",
    "postcss": "^8.5.1",
    "tailwindcss": "^4.0.3"
  }
}
